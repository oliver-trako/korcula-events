{
  "name": "korcula-events",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:js": "terser site/js/app.js --compress --mangle -o site/js/app.min.js",
    "build:css": "cleancss -o site/css/style.min.css site/css/style.css",
    "build": "npm run build:css && npm run build:js && node scripts/build-site.mjs"
  },
  "devDependencies": {
    "clean-css-cli": "^5.6.3",
    "terser": "^5.49.0"
  }
}
